# Marta Sierra Obea — CV

A single-page, terminal-themed personal CV.

## View it live

Once GitHub Pages is enabled for this repo (Settings → Pages → Deploy from branch → `main` / `/ (root)`), it will be published at:

`https://<your-username>.github.io/<repo-name>/`

## Local preview

Just open `index.html` in a browser — no build step required.
